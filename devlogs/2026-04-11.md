# SPS devlog — 2026-04-11

## [00:00 UTC] Session

### State at Session Start

85/85 tests passing. All three prioritized SPS features complete: adversarial SPS
(AdversarialEmbeddingFamily), spectral gap LOO (loo_spectral_gap), bootstrap CIs +
delta-method rSPS CI (SPSReport.gap_ci / rsps_ci). GPU runs blocked by environment.

### Work Completed

Orientation review only — no changes to SPS this session. All code work for this
iteration confirmed complete. Primary session effort directed to NeuroVoice
(PortugueseArticulatoryMapper, 89 tests, 1489/1489 passing).

### Results / Observations

SPS library: 85/85 tests passing. No regressions.

### Hypotheses

No new hypotheses.

### Next Steps

- GPU run: `python experiments/estimate_sps.py --epsilon 0.1 --m-transforms 24 --n-boot 1000` on RoBERTa-base
- GPU run: `full_sps_analysis()` end-to-end integration test with RoBERTa-base

---

## [04:30 UTC] Session

### State at Session Start

85/85 tests passing. All three SPS priorities complete: AdversarialEmbeddingFamily,
loo_spectral_gap, SPSReport.gap_ci / rsps_ci. GPU runs blocked by environment.

### Work Completed

Orientation review only — no code changes to SPS this session. Primary effort
directed to metaMirage (README polish, dashboard LOO section) and NeuroVoice
(PolishArticulatoryMapper).

### Results / Observations

SPS library: 85/85 tests passing. No regressions.

### Hypotheses

No new hypotheses.

### Next Steps

- GPU run: `python experiments/estimate_sps.py --epsilon 0.1 --m-transforms 24 --n-boot 1000` on RoBERTa-base
- GPU run: `full_sps_analysis()` end-to-end integration test with RoBERTa-base

---

## [09:22 UTC] Session

### State at Session Start

85/85 tests passing. All three SPS priorities complete: AdversarialEmbeddingFamily,
loo_spectral_gap, SPSReport.gap_ci / rsps_ci. GPU runs blocked by environment.

### Work Completed

Orientation review only — no changes to SPS this session. Primary session effort
directed to NeuroVoice (CzechArticulatoryMapper, 91 tests, 1755/1755 passing).

### Results / Observations

SPS library: 85/85 tests passing. No regressions.

### Hypotheses

No new hypotheses.

### Next Steps

- GPU run: `python experiments/estimate_sps.py --epsilon 0.1 --m-transforms 24 --n-boot 1000` on RoBERTa-base
- GPU run: `full_sps_analysis()` end-to-end integration test with RoBERTa-base

---

## [10:49 UTC] Session

### State at Session Start

85/85 tests passing. All three SPS priorities complete: AdversarialEmbeddingFamily,
loo_spectral_gap, SPSReport.gap_ci / rsps_ci. GPU runs blocked by environment.

### Work Completed

Orientation review only — no code changes to SPS this session. Primary effort
directed to NeuroVoice (SwedishArticulatoryMapper, 93 tests, 1848/1848 passing).

### Results / Observations

SPS library: 85/85 tests passing. No regressions.

### Hypotheses

No new hypotheses.

### Next Steps

- GPU run: `python experiments/estimate_sps.py --epsilon 0.1 --m-transforms 24 --n-boot 1000` on RoBERTa-base
- GPU run: `full_sps_analysis()` end-to-end integration test with RoBERTa-base

---

## [11:26 UTC] Session

### State at Session Start

85/85 tests passing. AdversarialEmbeddingFamily, loo_spectral_gap, and
SPSReport.gap_ci / rsps_ci all complete. GPU runs blocked by environment.

### Work Completed

Orientation review only — no code changes to SPS this session. Primary effort
directed to NeuroVoice (NorwegianArticulatoryMapper + FinnishArticulatoryMapper;
2016/2016 pass).

### Results / Observations

SPS library: 85/85 tests passing. No regressions.

### Hypotheses

No new hypotheses.

### Next Steps

- GPU run: `python experiments/estimate_sps.py --epsilon 0.1 --m-transforms 24 --n-boot 1000` on RoBERTa-base
- GPU run: `full_sps_analysis()` end-to-end integration test with RoBERTa-base

---

## [13:21 UTC] Session

### State at Session Start

85/85 tests passing. AdversarialEmbeddingFamily, loo_spectral_gap, and
SPSReport.gap_ci / rsps_ci all complete. GPU runs blocked by environment.

### Work Completed

Orientation review only — no code changes to SPS this session. Primary effort
directed to NeuroVoice (GreekArticulatoryMapper, /θ ð x ɣ/ dental+velar
fricatives, 25 phonemes, 82 tests, 2176/2176 pass).

### Results / Observations

SPS library: 85/85 tests passing. No regressions.

### Hypotheses

No new hypotheses.

### Next Steps

- GPU run: `python experiments/estimate_sps.py --epsilon 0.1 --m-transforms 24 --n-boot 1000` on RoBERTa-base
- GPU run: `full_sps_analysis()` end-to-end integration test with RoBERTa-base

---

## [14:48 UTC] Session

### State at Session Start

85/85 tests passing. AdversarialEmbeddingFamily, loo_spectral_gap, and
SPSReport.gap_ci / rsps_ci all complete. GPU runs blocked by environment.

### Work Completed

Orientation review only — no code changes to SPS this session. Primary effort
directed to NeuroVoice (HungarianArticulatoryMapper, palatal stops /c ɟ/,
/ɒ/ back unrounded, 14 vowels, 38 phonemes, 91 tests, 2267/2267 pass).

### Results / Observations

SPS library: 85/85 tests passing. No regressions.

### Hypotheses

No new hypotheses.

### Next Steps

- GPU run: `python experiments/estimate_sps.py --epsilon 0.1 --m-transforms 24 --n-boot 1000` on RoBERTa-base
- GPU run: `full_sps_analysis()` end-to-end integration test with RoBERTa-base

---

## [16:24 UTC] Session

### State at Session Start

T-5 days to April 16 deadline. Package confirmed submission-ready from all
prior sessions. 8/8 notebook cells executed. dashboard.html 750 lines with
LOO stability section. README.md 180 lines with accurate file tree.
per_family_scatter.png (91 KB) generated. kaggle_task.py --demo passes 8 tasks.

### Work Completed

Orientation review only — no changes to metaMirage this session. Primary effort
directed to NeuroVoice (RussianArticulatoryMapper, 42 phonemes, 4 signatures:
/ɨ/ back unrounded, palatalization system /Cʲ/ vs /C/, /ʐ/ retroflex,
/ɕ/ always-soft palatal; 88 tests, 2355/2355 pass).

### Results / Observations

Package status: submission-ready. No regressions. All checks pass.

### Hypotheses

No new hypotheses.

### Next Steps

- Final pre-submission check: April 14–15
- Submit to Kaggle by April 16

---

## [18:49 UTC] Session

### State at Session Start

metaMirage: submission-ready (T-5 days to April 16). 8/8 notebook cells executed.
dashboard.html 750 lines with LOO stability section. README.md 180 lines accurate.
per_family_scatter.png (91 KB) generated. kaggle_task.py --demo passes 8 tasks.

SPS: 85/85 tests passing. All prioritized features complete. GPU runs blocked.
NeuroVoice: multilingual mapper series (25 languages, 2380 baseline tests).
AlphaNexus, rootslink: no code work needed.

### Work Completed

Orientation only — no changes to this repository this session. Primary effort
directed to NeuroVoice (HindiArticulatoryMapper, 50 phonemes, 4-way stop contrast,
dental/retroflex distinction, /ɽ/ retroflex flap, 83 tests, 2463/2463 pass).

### Results / Observations

Package status: submission-ready (metaMirage). No regressions (SPS 85/85).
NeuroVoice full suite: 2463/2463 pass.

### Hypotheses

No new hypotheses.

### Next Steps

- metaMirage: final pre-submission check April 14–15; submit by April 16
- NeuroVoice: next mapper (Catalan, Swahili, or Bengali)